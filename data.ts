// 默认选择环境和小程序版本即可
const defaults = {
  env: 'uat',
  version: 'dev',
  channel: 'weapp',
};

console.log('%c AT-[ defaults ]-2', 'font-size:13px; background:#de4307; color:#f6d04d;', defaults);

// 后端知道版本后，
